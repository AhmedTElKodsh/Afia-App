we modified the workflow from the complex auto-detect, auto-cature to just using the outline like @/d:/AI Projects/Freelance/Afia-App/oil-bottle-frames/bottle-camera-outline2.png just as a static shape with no associated functionality like the text merely as guidance for the user 

Local Logo Verification
   * Method: Template Matching (using OpenCV.js) or a lightweight MobileNetV2-based model (TensorFlow.js) running locally.
   * Logic: It looks for the "Afia" branding (Arabic/English) within the locked frame.
   * Fail-safe: If logo detection fails locally but geometry is perfect, it proceeds to the analysis phase but flags the image as "Low Logo Confidence" in the  
     Admin Dashboard.





/bmad-help use suitable agents to review the workflow from the start:
1. the user scans the barcode on the afia oil bottle (we should have different mock qr codes for 1.5 litter and 2.5 litter since they have the same shape but different sizes, geometries, while we will only focus on 1.5 litter bottles for now)
2. scanned barcode, leads to the Cloudflare deployed weblink, once opened the user is triggered to open the camera to photograph the bottle 
3. once the camera is opened (to the frontside of the bottle, a general message directing the user to shoot the frontside not the backside or the left/right sides), an exact shape outline for the bottle appears on the screen, and the user is directed to move closer or farther, also adjust the angle (upper or lower) till the camera-drawn outline perfectly matches the bottle, once it matches the shape turns from red or orange to green and the camera auto-lock, auto-take the photo (the user doesn't need to press the camera button)
4. due to the fact that there are multiple oil brands with the same shape and same oil size of 1.5, we also need the camera after it confirms the outline matching the shape, it also confirms the "Afia" (for now using the easiest way to detect the Afia either the whole logo or specifically the Afia Arabic and English word if technically possible and easy now)
as mentioned the camera will detect if the outline matches so if the customer is shooting in a bad conditions and the camera won't take a "bad" photo, it will direct the user to enhance the lightening or position (add this in the implementation) and even if the local mode for any reasons failed to analyze the photo, the direct action will be to direct the user to retake the photo (but such errors should be logged and saved on the admin side to review later). currently in this MVP stage after the POC and before the launch, use the 3 Stages Strategy you proposed till the local model is refined enough with acceptable accuracy for the launch.


/bmad-help use suitable agents to: review the following part of the workflow, now after the oil bottle image is caught by the camera: and get analyzed through sending a prompt carrying text direction along with the bottle image to be tested and reference/guiding data (or image to clarify the bottle geometry and few-shots image examples handled in special way for size & token reduction) through LLM APIs (free-tier multiple Gemini keys with Grok as fallback) which is the target of the POC/Prototype phase, while we will use the real images (extracted from video frames) as well as the AI-Augmented images (based on the real images) creating a Supabase database to train a local lightweight model that will be able to run on any mobile browser which will be used as the main route for analyzing the bottle image and expecting the oil level with the LLM API as fallback mechanism if the analysis failed, the request is sent to the LLM API and the image along with the local model result and the LLM API fallback result are saved in the Admin dashboard for revision. and if the analysis is performed on the Admin side, the data is also saved in the dashboard, but also the admin can click in the same time, on one of the buttons denoting that the measure is not accurate (too big or too small) and either add the correct result manually or also send the LLM API fallback, note that we also want the Admin to be able to upload an image along with the metadata and oil level, the should make use of all these results and data in training, refining and enhancing the local model.
the result to the user or Admin should show the imaged oil bottle with the oil level marked with red line and the consumed & remaining oil mentioned as text below, on the right ot left side of the bottle, a slider should appear starting at exactly the red line on the remaining oil level, with an inner circle/shape which the user can control and move by his thumb across the slider upper or lower where each shape move across the slider represents 55ml on the bottle and if the shape reached the end of the slider and the remaining is less than 55ml, the shape stops at the last 55ml level, below the slider, as 55ml represents 1/2 tea cup, when the user moves the shape or the scroll by 55 ml, a cup half filled is show below the slider, and if the user moved 110ml, the cup should be fully field, and if the user moved another 55ml, the cup appears again half field but beside the shape, the number of cup consumed (so 1/2 Cup then 1 Cup then 1,1/2 cup then 2 Cups then 2,1/2 Cups and so on)I'm thinking about dividing the project into 2 levels/stages, 1st level/stage calls the LLM APIs directly for the analysis, refine, enhance the prompt to enhance the accuracy of the PI response from the first take, then test, debug and deploy. meanwhile, use the images database to train, develop, refine the local model, so for the 2nd level/stage is to use the local model as the primary model and the LLMs APIs as fallback after testing that the local model reaches acceptable level of accuracy 









add Autoresearch https://github.com/karpathy/autoresearch, 

Playwright, RAG-Anything, grill-me, superpowers, Context7, Tavily, Web Artifacts Builder, Agent-Skills-for-Context-Engineering,       
      https://github.com/199-biotechnologies/claude-deep-research-skill skills, https://github.com/remotion-dev/remotion,
      https://skillhub.club/skills/molt-bot-clawdbot-video-frames, https://skillhub.club/skills/anthropics-claude-code-frontend-design, tailwind-v4,       
   also
      add 21stdev mcp using API = an_sk_1678b7d97bcc0d379919e05feed2a07df4a1881a7af6066bc7db6d531e32b07b 
add 21stdev mcp using API = an_sk_1678b7d97bcc0d379919e05feed2a07df4a1881a7af6066bc7db6d531e32b07b 


---

✅ COMPLETED - Screen 4b: Fill Confirmation with Usage Tracker (2026-04-26)
- Standard tea cup = 220ml, so 55ml = 1/4 cup
- Slider behavior: User drags DOWN to show oil being TAKEN OUT
- Display shows: "You're taking out: Xml = Y cups" + "Will remain: Zml = W cups"
- Slider range: Top = current level (red line), Bottom = last 55ml mark
- Edge case: If <55ml remaining, slider disabled with message
- Cup visualization: Single cup icon that fills progressively, resets at each full cup
- Real-time updates as user drags slider
- Haptic feedback every 55ml milestone
- See: _bmad-output/planning-artifacts/ux-design-specification.md (Screen 4b section)
