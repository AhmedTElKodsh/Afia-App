# Cloudflare Worker URL (no trailing slash)
# For local dev, this points to wrangler dev server
VITE_PROXY_URL=http://localhost:8787

# For production deployment, set this in Cloudflare Pages dashboard:
# VITE_PROXY_URL=https://safi-worker.your-subdomain.workers.dev
